# azat.life

Личный сайт Азата Хусаенова — технический директор мероприятий.

Статика без сборки: `index.html` + `css/style.css`. Хостинг — GitHub Pages, домен `azat.life` (файл `CNAME`).

## Локальный просмотр

Открыть `index.html` в браузере — этого достаточно, сборки нет.

## Деплой

1. Публичный репо на GitHub, пуш содержимого папки.
2. Settings → Pages → Deploy from branch (`main`, корень).
3. У регистратора домена: A-записи GitHub Pages для `azat.life` + CNAME `www` → `<username>.github.io`.
4. В Settings → Pages вписать custom domain `azat.life`, включить Enforce HTTPS.

## Логотипы клиентов

`assets/logos/` — белый монохром на прозрачном фоне. Каждый файл лежит на холсте высотой
150 px и отцентрован внутри него, поэтому в CSS у всех одна `height: 44px` и они стоят
на общей линии. Размер внутри холста подобран на глаз: лок-апы «значок + подпись»
читаются мельче однострочных надписей той же высоты, поэтому у них он больше.
Выводятся с `opacity: .5`, при наведении — полная непрозрачность.

Источники: сайт ETS Production, Wikimedia Commons, официальные материалы брендов.

## Фото

`assets/azat.jpg` — кроп 400×400 из оригинала `IMG_3164.JPG` (Nikon D5000, 1899×2848).
Показывается в hero на 200 px, то есть ровно 2×. Кроп взят по голове и плечам: ниже
в кадр попадает надпись на футболке, а она в оригинале зеркальная и читается задом наперёд.
Если менять кадрирование — держать её за границей.

## TODO

- Деплой: репозиторий на GitHub, Pages, DNS для `azat.life`.
- EN-версия (`/en/`) — структура одностраничника переносится как есть.

Почту решили не публиковать — единственный контакт это Telegram.
